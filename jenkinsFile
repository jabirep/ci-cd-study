pipeline {
    agent any

    tools {
        maven 'Maven' // configure in Jenkins global tool config
        jdk 'Java17'  // configure in Jenkins global tool config
    }

    stages {
        stage('Checkout') {
            steps {
                git branch: 'main', credentialsId: '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' url: 'https://github.com/jabirep/ci-cd-study.git'
            }
        }

        stage('Build & Test') {
            steps {
                bat 'mvn clean test'
            }
        }
    }
}
